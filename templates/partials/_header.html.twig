<header>
	<nav class="navbar d-flex justify-content-between">
		<div class="d-flex px-2 align-items-center">
			<a href="{{ path('home.index') }}"><img class="logo" src="{{ asset('/images/logovynile.png') }}" alt="Logo"></a>
			<a class="nav-link active text-white mx-2" href="{{ path('home.index') }}">Accueil</a>
			<a class="nav-link active text-white mx-2" href="{{ path('app_register') }}">Inscription</a>
			<a class="nav-link active mx-2 text-white" id="deco3" href="{{ path('app_login') }}">Connexion</a>
			<a class="nav-link active text-white mx-2" href="{{ path('app_disque_index') }}">Produits</a>
			<a class="nav-link active text-white mx-2" href="{{ path('app_artiste_index') }}">Artistes</a>
			<a class="nav-link active text-white mx-2" href="{{ path('cart_index') }}"><img src="{{ asset('/images/cart-shopping-svgrepo-com.svg') }}" alt="panier" class="cart filterInvert" id="panier"></a>
		</div>
		<div class="d-flex px-2 align-items-center">
			{% if app.user %}
				<div class="nav-item dropdown">
					<a class="nav-link dropdown-toggle text-white mx-2" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Compte
					</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
						<li>
							<a class="dropdown-item" href="{{path('app_user_show', {id: app.user.id})}}">Profil</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_orders_show') }}">Commandes</a>
						</li>
						<li>
							<a class="dropdown-item" id="deco" href="{{ path('app_logout') }}">Se déconnecter</a>
						</li>
						{% if is_granted('ROLE_ADMIN') %}
							<li>
								<a class="dropdown-item" href="{{path('admin')}}">Panel admin</a>
							</li>
						{% endif %}
					</ul>
				</div>
			{% endif %}

		</div>
		<div>
			{% if app.user %}
				<div class="text-white">Bonjour
					{{ app.user.nom }}
					{{ app.user.prenom }}</div>
			{% endif %}
		</div>
	</nav>
</header>
